<% if current_user.id != @user.id %>
<div class="panel panel-default">
	<div class="panel-body">
		<center>
			<%  if !current_user.blocking?(@user) %>

			<%= form_for(current_user.active_blockingships.build) do |f| %>
			<div><%= hidden_field_tag :blocked_id, @user.id  %></div>
			<%= f.submit "Engelle", class: "btn btn-primary"  %>
			<% end %>
			<% else %>
			<%= form_for(current_user.active_blockingships.find_by(blocked_id: @user.id), html: {method: :delete}) do |f| %>
			<%= f.submit "Engeli Kaldır", class: "btn"  %>

		<% end %>
		<% end %>





		</center>
	</div>
</div>
<% end %>