<h1>Profili Düzenle</h1>

<%= form_for(@user, html: {multipart: true}) do |f| %>
<%= f.label :username %> 
<%= f.text_field :username %><br><br>
<%= f.file_field :avatar %><br><br>

<%= f.submit "Değişiklikleri Kaydet" %>
<% end %>